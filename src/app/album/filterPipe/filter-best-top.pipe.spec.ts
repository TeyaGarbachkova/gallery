import { FilterBestTopPipe } from './filter-best-top.pipe';

describe('FilterBestTopPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBestTopPipe();
    expect(pipe).toBeTruthy();
  });
});
