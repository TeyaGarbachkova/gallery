import { FilterBestPipe } from './filter-best.pipe';

describe('FilterBestPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBestPipe();
    expect(pipe).toBeTruthy();
  });
});
