import { FilterLetterPipe } from './filter-letter.pipe';

describe('FilterLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
